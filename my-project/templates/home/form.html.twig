{% extends 'base.html.twig' %}


{% block body %}

    <h3 class="jumbotron text-center mt-4"> N'hesitez pas à nous contacter </h3>
    <div class="container mt-4">
                {{form_start(form)}}
                {{form_widget(form)}}
                {{form_end(form)}}
    </div>

{% endblock %}